export type Guest = {
  id: string;
  firstName: string;
  lastName: string;
  householdId: string;
};

export type GuestCredentials = {
  firstName: string;
  lastName: string;
  password: string;
};
