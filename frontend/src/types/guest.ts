export type Guest = {
  id: number;
  firstName: string;
  lastName: string;
  householdId: number;
};

export type GuestCredentials = {
  firstName: string;
  lastName: string;
  password: string;
};
